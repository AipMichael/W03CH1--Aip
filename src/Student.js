import DeathEater from "./DeathEater.js";
import Personaje from "./Personaje.js";

class Student extends Personaje {
    auror;
    pelotismo;

    constructor(nombre, familia, edad, auror, pelotismo) {
        super(nombre, familia, edad);
        this.comunicado = "It's le-vi-o-sa, not leviosA";
        this.pelotismo = pelotismo;

        if (luchador instanceof DeathEater) {
            this.auror = auror;
        }
    }
}

export default Escudera;
